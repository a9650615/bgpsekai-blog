name = "baha-cors-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Deploy: npx wrangler deploy
# Test:   npx wrangler dev
#
# After deploy, update PROXY_BUILDERS in src/lib/baha-danmu/api.ts:
#   (t) => `https://baha-cors-proxy.<your-subdomain>.workers.dev/?sn=${sn}`
#
# Custom domain (optional):
# Add a route in CF dashboard: api.bgpsekai.club/baha/* → baha-cors-proxy
